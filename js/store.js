function initStore() { const c = document.getElementById('categorySections'); if (!c) return; c.innerHTML = ''; const cats = [...new Set(window.products.map(p => p.category))]; cats.forEach(cat => { const s = document.createElement('div'); s.className = 'category-section'; const ps = window.products.filter(p => p.category === cat); s.innerHTML = '<h2 class="section-title">' + cat + '</h2><div class="products-grid">' + ps.map(p => '<div class="product-card"><div class="product-image"><img src="' + p.thumbnail + '" alt="' + p.name + '"></div><div class="product-info"><div class="product-name">' + p.name + '</div><div class="product-price">Rs.' + p.price + '</div><button onclick="addToCart(' + p.id + ')" class="add-to-cart-btn">Add to Cart</button></div></div>').join('') + '</div>'; c.appendChild(s); }); } document.addEventListener('DOMContentLoaded', () => { if (window.products && window.products.length) initStore(); });
