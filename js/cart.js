let cart = JSON.parse(localStorage.getItem('madeInMyDeesaCart')) || []; function updateCartCount() { const el = document.getElementById('cartCount'); if (el) el.innerText = cart.length; } function addToCart(id, qty = 1) { const p = window.products.find(x => x.id == id); if (!p) return; const ex = cart.find(x => x.id == id); if (ex) ex.quantity += qty; else cart.push({...p, quantity: qty}); saveCart(); updateCartCount(); renderCart(); openCart(); }function saveCart() { localStorage.setItem('madeInMyDeesaCart', JSON.stringify(cart)); } function renderCart() { const c = document.getElementById('cartItems'); if (!c) return; c.innerHTML = cart.length ? cart.map(i => `<div class="cart-item"><div class="cart-item-info"><div class="cart-item-name">${i.name}</div><div class="cart-item-price">Rs.${i.price} x ${i.quantity}</div></div><button onclick="removeFromCart(${i.id})" class="cart-item-remove">Remove</button></div>`).join('') : '<div class="empty-cart">Your cart is empty</div>'; document.getElementById('cartTotal').innerText = 'Rs.' + cart.reduce((s, i) => s + (i.price * i.quantity), 0); }function removeFromCart(id) { cart = cart.filter(x => x.id != id); saveCart(); updateCartCount(); renderCart(); } function openCart() { document.getElementById('cartSidebar').classList.add('open'); document.getElementById('cartOverlay').classList.add('open'); } function closeCart() { document.getElementById('cartSidebar').classList.remove('open'); document.getElementById('cartOverlay').classList.remove('open'); } document.addEventListener('DOMContentLoaded', () => { updateCartCount(); renderCart(); document.getElementById('cartBtn')?.addEventListener('click', openCart); document.getElementById('closeCart')?.addEventListener('click', closeCart); document.getElementById('cartOverlay')?.addEventListener('click', closeCart); });
